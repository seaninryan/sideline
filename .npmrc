; @vercel/analytics declares @sveltejs/kit as an optional peer, which npm tries to
; satisfy with a Svelte + vite@8 chain that conflicts with our vite@5 (from Vitest).
; We're a Next.js app — that peer is irrelevant — so ignore peer-dep resolution.
legacy-peer-deps=true
